/**
 * Where somebody without Coilbox goes to get it.
 *
 * The project site rather than `releases/latest`, because it says what Coilbox
 * is before it offers a binary, and it carries the per OS install steps, which
 * a bare asset list does not.
 */
export const COILBOX_URL = "https://tomjn.github.io/coilbox/";
